spring.application.name=springapp

spring.datasource.url=jdbc:mysql://localhost:3306/springapp
spring.datasource.username=root
spring.datasource.password=root

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

server.port=8080
Why it fails

In your Feedback entity, there is no field named manager.

But your repository method name expects a field manager inside Feedback, and then a nested field userId.

Example Spring Data logic:

findByManager_UserId(...) → looks for feedback.manager.userId

# JPA/Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true


# Enable Swagger OpenAPI generation
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html


spring.security.user.name=admin
spring.security.user.password=admin123
spring.security.user.roles=ADMIN


